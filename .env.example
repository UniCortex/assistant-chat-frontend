API_BASE_URL=url # без слеша в конце (например, https://api.somesite.com)
BASE_PATH=base_path # например, /
HOST_PORT=host_port # например, 5173
PORT=port # например, 5173
CHAT_SESSION_STORAGE_KEY=storage_key # например, assistant-chat-session
CHAT_SESSION_TTL_HOURS=ttl_hours # например, 12
SERVER_HOST=server_host # например, 0.0.0.0
